# Core web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database & migrations
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.9

# Authentication & security
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # pinned: passlib 1.7.4 is incompatible with bcrypt >= 4.1
python-multipart==0.0.20

# File ingestion (CSV via stdlib, XLSX via openpyxl)
openpyxl==3.1.5

# Async jobs
celery==5.4.0
redis==5.2.1

# Outbound HTTP (webhook delivery)
httpx==0.28.1
